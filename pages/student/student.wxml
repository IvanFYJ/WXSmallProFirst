<!--pages/student/student.wxml-->
<view class="container">
  <label>{{gname}}</label>
    <view class='itemView' >
    <button class='fbtn'  wx:for="{{students}}" wx-for-item="item" bindtap='payforstudent' data-id='{{item.sid}}' data-snumber='{{item.snumber}}'  >{{item.sname}}</button>
    </view>
</view>
